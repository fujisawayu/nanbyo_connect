<body class="login_image">
  <div class="memox huifont">
    <ol>
      <li>難病に関する治療内容や治療費が共有されています。</li>
      <li>難病と診断された際の助成金の目安を知ることができます。</li>
      <li>自分自身が、大切な人が、難病と診断された際に知りたい情報が集まっています。</li>
    </ol>
  </div>
  <div class="box">
    <div class="box-inner">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <h3>ログイン</h3>
      <div class="box-email">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>
      <div class="box-password">
        <div class="a-row">
          <div class="column-left">
            <%= f.label :password %>
          </div>
          <div class="column-right">
            <%= link_to "パスワードを忘れた場合", new_user_password_path %>
          </div>
        </div>
        <%= f.password_field :password, autocomplete: "current-password" %>
      </div>
      <div class="box-login">
        <%= f.submit "ログイン" %>
        <%= render "devise/shared/links" %>
        <% if devise_mapping.rememberable? %>
        <div class="remember">
          <%= f.check_box :remember_me %>
          <div style="text-align:center">
            <%= f.label :remember, "ログインしたままにする" %></div>
        </div>
        <% end %>
      </div>
      <% end %>
      <div class="box-newusr">
        <%= link_to "アカウントを作成", new_user_registration_path %>
      </div>
    </div>
  </div>
  <br>
</body>