<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Name</th>
      <th>Birthday</th>
      <th>Prefecture</th>
      <th>Self introduction</th>
      <th>Image</th>
      <th>Classification</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.name %></td>
      <td><%= user.birthday %></td>
      <td><%= user.prefecture %></td>
      <td><%= user.self_introduction %></td>
      <td><%= user.image %></td>
      <td><%= user.classification %></td>
      <td><%= link_to '詳細', user %></td>
      <td><%= link_to '編集', edit_user_path(user) %></td>
      <td><%= link_to '削除', user, method: :delete, data: { confirm: 'のエラーがあります' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>